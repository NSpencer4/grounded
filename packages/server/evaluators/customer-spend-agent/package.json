{
  "name": "@grounded/customer-spend-agent",
  "version": "0.0.1",
  "description": "AI agent for analyzing customer spending data",
  "type": "module",
  "main": "dist/handler.js",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "package": "yarn run build && cd dist && zip -r function.zip .",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix"
  },
  "dependencies": {
    "@grounded/agents-shared": "*",
    "@grounded/schemas": "*",
    "@grounded/server-shared": "*"
  }
}
