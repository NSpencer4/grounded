{
  "name": "@grounded/conversations-responder",
  "version": "0.0.1",
  "description": "Lambda responder for processing conversation assertions and producing updates",
  "type": "module",
  "main": "dist/handler.js",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "package": "npm run build && cd dist && zip -r function.zip .",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix"
  },
  "dependencies": {
    "@grounded/schemas": "*",
    "@grounded/server-shared": "*"
  }
}
